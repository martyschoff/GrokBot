# GrokBot

App sources for Martin's Grok Bot projects. App trees live under `apps/<app>/`.

The English NT player lives at `apps/nt4me2/` (user-facing chrome title: living words). Live audio on S3 `nt4me2` stays with Kimberly — this repo is chrome/player only.

See `apps/nt4me2/README.md` for Settings, Berean/KJV × Voice sew paths, Galatians 1–2 fixtures, and how Kimberly wires real fragment mp3s / `live.json` keys.
